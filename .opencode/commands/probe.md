# /probe command

Read-only diagnosis: `adb devices`, filtered logcat tail, or short hub-python probes of fixtures/logs. No installs, no builds, no edits. Summarize: observed vs expected + smallest next test. Large files via scripted probe, never full ingest.
