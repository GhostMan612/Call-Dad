# /smoke command

Human-device smoke script (human runs in Studio/on-device, executor records pasted evidence):
1. Launch → giant Call Dad visible → tap → Dad rings (note latency + network: LAN/hotspot/remote).
2. 30s voice → hangup → redial. 3. Send text + voice memo → receipts. 4. Send photo → received + verified.
5. Airplane+WiFi recovery. 6. No-escape walk (back button, no browser/store/settings exit).
Record: PASS/FAIL per line + device + build flavor. Executor never claims; only transcribes human evidence into CURRENT_STATE.
